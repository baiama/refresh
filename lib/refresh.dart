import 'package:flutter/material.dart';

class VibrationRefreshIndicator extends StatelessWidget {
  const VibrationRefreshIndicator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
